# Antigravity Handover & Resume Guide

This file provides the status of the current deployment and bKash integration tasks for the next Antigravity session.

## 1. What Has Been Completed & Fixed
* **bKash Headers Casing & Accept Header Fix:**
  * Added the missing `Accept: application/json` header in [paymentController.js](file:///home/shezan/Projects/Orpon/backend/controllers/paymentController.js) for all bKash API calls.
  * Added `BKASH_BASE_URL` and `SSLCOMMERZ_BASE_URL` to required environment variable checks in [env.js](file:///home/shezan/Projects/Orpon/backend/config/env.js) to catch missing configurations early.
* **Azure Deployment Fix (Basic Auth):**
  * SCM and FTP basic authentication were disabled on Azure, which caused the GitHub Action publish profile deployment to fail. 
  * We enabled them via Azure CLI:
    ```bash
    az resource update --resource-group OrponRG --name scm --namespace Microsoft.Web --resource-type basicPublishingCredentialsPolicies --parent sites/orpon-backend-api-sea --set properties.allow=true
    ```
* **Workflow Modernization:**
  * Updated [.github/workflows/azure-web-app-backend.yml](file:///home/shezan/Projects/Orpon/.github/workflows/azure-web-app-backend.yml) to use v4 actions (`actions/checkout@v4`, `actions/setup-node@v4`) to resolve Node 20 deprecation warnings.

---

## 2. Current Status of the App
* Direct Azure CLI zip deployment was verified successfully.
* The backend is **running and healthy** on Azure. Hitting the health check returns success:
  * URL: `https://orpon-backend-api-sea.azurewebsites.net/api/health`
  * Response: `{"status":"ok", "timestamp": "..."}`

---

## 3. Current Blockers (bKash Sandbox Outage)
* **bKash Sandbox API is down:** Hitting the `/checkout/create` endpoint returns `{"statusCode":"","statusMessage":""}` and the bKash Merchant Demo website at `https://merchantdemo.sandbox.bka.sh/` returns a `502 Bad Gateway`.
* **Credentials are correct:** We verified that Token Grant (`/token/grant`) is working (handled by Cognito), and all header variations (`X-App-Key`, `X-APP-Key`, `x-app-key`) have been fully verified.

---

## 4. Next Steps
1. **GitHub Secret Update:**
   * The unredacted publish profile XML has been saved to [publish_profile_temp.xml](file:///home/shezan/Projects/Orpon/publish_profile_temp.xml) in the project root.
   * Copy the XML and update the `AZURE_WEBAPP_PUBLISH_PROFILE` secret on GitHub, then trigger the workflow. Once updated, the user should delete `publish_profile_temp.xml`.
2. **Verify bKash Integration:**
   * Once bKash restores the sandbox services, verify the payment flow by executing a `POST` request to `https://orpon-backend-api-sea.azurewebsites.net/api/payment/bkash/initiate`.
